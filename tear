#!/usr/bin/env sh
set -e;

if test $# -lt 1
	echo "Usage: tear git-url"
	exit 1
fi

mkdir -p $TEAR_DIR/packages $TEAR_DIR/bin $TEAR_DIR/lib

cd $TEAR_DIR/packages
fido $1

cd $TEAR_DIR
ln -s $FIDO_CLONED_TO_DIR/bin
ln -s $FIDO_CLONED_TO_DIR/lib
